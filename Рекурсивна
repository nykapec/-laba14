#include <iostream>
using namespace std;
int factorialRecursion(int num)
{
	if (num <= 1)
		return 1;

	return num * factorialRecursion(num - 1);
}

int main()
{
	for (int i = 0; i < 10; i++)
	{
		cout << i << "!" << " = " << factorialRecursion(i) << endl;
	}
}
