#include <iostream>
using namespace std;

void factorialSimple(int num)
{
	int result = 1;
	for (int i = 0; i < num; i++)
	{
		if (i != 0)
			result *= i;

		cout << i << "!" << " = " << result << endl;
	}
}
int main()
{
	factorialSimple(10);
}
